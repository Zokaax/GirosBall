# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Audio

Audio is powered by `expo-av`. Sounds are generated synthetically (WAV via base64 data URIs) in `src/audio/sounds.ts` — no external audio files required.
